import { Component } from '@angular/core';
import { Navbar } from '../../components/navbar/navbar';

@Component({
  selector: 'app-board',
  imports: [Navbar],
  templateUrl: './board.html',
})
export class Board {}
